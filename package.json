{
  "name": "copilot-memory-mcp-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Complete MCP server and VSCode extension for Copilot memory storage",
  "workspaces": [
    "server",
    "extension",
    "shared"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces",
    "clean": "npm run clean --workspaces",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces",
    "package": "npm run package --workspaces"
  },
  "devDependencies": {
    "@types/node": "^22.7.9",
    "typescript": "^5.6.3",
    "rimraf": "^6.0.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}